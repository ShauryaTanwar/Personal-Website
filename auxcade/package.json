{
  "name": "auxcade",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Seven music-powered arcade games, one listening profile.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
